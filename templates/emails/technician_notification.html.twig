<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            border-radius: 10px 10px 0 0;
            text-align: center;
        }
        .content {
            background: #f8f9fa;
            padding: 30px;
            border-radius: 0 0 10px 10px;
        }
        .vehicle-info {
            background: white;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 20px;
            border-left: 4px solid #667eea;
        }
        .vehicle-info h3 {
            margin-top: 0;
            color: #667eea;
        }
        .info-row {
            display: flex;
            padding: 8px 0;
            border-bottom: 1px solid #e9ecef;
        }
        .info-row:last-child {
            border-bottom: none;
        }
        .info-label {
            font-weight: bold;
            width: 150px;
            color: #6c757d;
        }
        .info-value {
            flex: 1;
        }
        .problem-description {
            background: #fff3cd;
            padding: 15px;
            border-radius: 8px;
            margin: 20px 0;
            border-left: 4px solid #ffc107;
        }
        .ai-analysis {
            background: white;
            padding: 20px;
            border-radius: 8px;
            margin-top: 20px;
        }
        .diagnosis {
            background: #e7f3ff;
            padding: 15px;
            border-radius: 8px;
            margin: 15px 0;
            border-left: 4px solid #0d6efd;
        }
        .actions {
            background: #d1e7dd;
            padding: 15px;
            border-radius: 8px;
            margin: 15px 0;
            border-left: 4px solid #198754;
        }
        .actions ul {
            margin: 10px 0;
            padding-left: 20px;
        }
        .actions li {
            margin: 8px 0;
        }
        .urgency {
            display: inline-block;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: bold;
            margin: 10px 0;
        }
        .urgency-high {
            background: #f8d7da;
            color: #842029;
        }
        .urgency-medium {
            background: #fff3cd;
            color: #664d03;
        }
        .urgency-low {
            background: #d1e7dd;
            color: #0f5132;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 2px solid #e9ecef;
            color: #6c757d;
            font-size: 14px;
        }
        .badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: bold;
        }
        .badge-maintenance {
            background: #ffc107;
            color: #000;
        }
        .badge-hors-service {
            background: #dc3545;
            color: white;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🔧 Nouvelle Intervention Requise</h1>
        <p>PackTrack - Système de Gestion de Véhicules</p>
    </div>

    <div class="content">
        <p>Bonjour <strong>{{ technician.prenom }} {{ technician.nom }}</strong>,</p>
        
        <p>Un véhicule nécessite votre intervention. Voici les détails :</p>

        <div class="vehicle-info">
            <h3>📋 Informations du Véhicule</h3>
            <div class="info-row">
                <span class="info-label">Marque/Modèle:</span>
                <span class="info-value"><strong>{{ vehicule.marque }} {{ vehicule.modele }}</strong></span>
            </div>
            <div class="info-row">
                <span class="info-label">Immatriculation:</span>
                <span class="info-value">{{ vehicule.immatriculation }}</span>
            </div>
            <div class="info-row">
                <span class="info-label">Type:</span>
                <span class="info-value">{{ vehicule.typeVehicule }}</span>
            </div>
            <div class="info-row">
                <span class="info-label">Capacité:</span>
                <span class="info-value">{{ vehicule.capacite }} kg</span>
            </div>
            <div class="info-row">
                <span class="info-label">Statut:</span>
                <span class="info-value">
                    {% if vehicule.statut == 'en_maintenance' %}
                        <span class="badge badge-maintenance">En Maintenance</span>
                    {% else %}
                        <span class="badge badge-hors-service">Hors Service</span>
                    {% endif %}
                </span>
            </div>
        </div>

        <div class="problem-description">
            <h4>⚠️ Description du Problème</h4>
            <p>{{ analysis.originalDescription }}</p>
        </div>

        <div class="ai-analysis">
            <h3>🤖 Analyse IA du Problème</h3>
            
            <div class="diagnosis">
                <h4>🔍 Diagnostic</h4>
                <p>{{ analysis.diagnosis }}</p>
            </div>

            <div class="actions">
                <h4>✅ Actions Recommandées</h4>
                <ul>
                    {% for action in analysis.actions %}
                        <li>{{ action }}</li>
                    {% endfor %}
                </ul>
            </div>

            <div style="margin: 20px 0;">
                <strong>⏱️ Temps Estimé:</strong> {{ analysis.estimatedTime }}
            </div>

            <div>
                <strong>🚨 Niveau d'Urgence:</strong>
                {% set urgencyClass = 'urgency-medium' %}
                {% if analysis.urgency == 'Élevé' %}
                    {% set urgencyClass = 'urgency-high' %}
                {% elseif analysis.urgency == 'Faible' %}
                    {% set urgencyClass = 'urgency-low' %}
                {% endif %}
                <span class="urgency {{ urgencyClass }}">{{ analysis.urgency }}</span>
            </div>
        </div>

        <p style="margin-top: 30px;">
            Merci de prendre en charge cette intervention dans les meilleurs délais.
        </p>

        <p>
            Cordialement,<br>
            <strong>L'équipe PackTrack</strong>
        </p>
    </div>

    <div class="footer">
        <p>Cet email a été généré automatiquement par le système PackTrack.</p>
        <p>Pour toute question, contactez l'administrateur système.</p>
    </div>
</body>
</html>
