<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}PackTrack - Admin{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/back-style.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <style>
        .sidebar ul li.has-submenu { position: relative; }
        .sidebar ul li.has-submenu > a { display: flex; justify-content: space-between; align-items: center; cursor: pointer; transition: all 0.3s ease; }
        .sidebar ul li.has-submenu > a:hover { padding-left: 25px; }
        .sidebar ul li.has-submenu .arrow { transition: transform 0.3s ease; font-size: 11px; opacity: 0.7; }
        .sidebar ul li.has-submenu.open .arrow { transform: rotate(180deg); }
        .submenu { max-height: 0; overflow: hidden; transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1); background: linear-gradient(to bottom, rgba(0,0,0,0.15), rgba(0,0,0,0.05)); margin: 0; padding: 0; list-style: none; border-radius: 0 0 8px 8px; opacity: 0; }
        .sidebar ul li.has-submenu.open .submenu { max-height: 200px; opacity: 1; margin-top: 5px; padding: 8px 0; }
        .submenu li { margin: 0; padding: 0; }
        .submenu li a { padding: 12px 20px 12px 45px; font-size: 13.5px; display: flex; align-items: center; transition: all 0.2s ease; border-left: 3px solid transparent; position: relative; }
        .submenu li a::before { content: ''; position: absolute; left: 28px; width: 4px; height: 4px; background: currentColor; border-radius: 50%; opacity: 0.5; }
        .submenu li a:hover { background: rgba(255,255,255,0.1); padding-left: 50px; border-left-color: rgba(255,255,255,0.5); }
        .submenu li a:hover::before { opacity: 1; transform: scale(1.3); }
        .submenu li.active a { background: rgba(255,255,255,0.15); border-left-color: #fff; font-weight: 500; }
        .submenu li a i { font-size: 13px; margin-right: 10px; width: 16px; text-align: center; opacity: 0.8; }
        @keyframes slideIn { from { opacity: 0; transform: translateX(-10px); } to { opacity: 1; transform: translateX(0); } }
        .sidebar ul li.has-submenu.open .submenu li { animation: slideIn 0.3s ease forwards; }
        .sidebar ul li.has-submenu.open .submenu li:nth-child(1) { animation-delay: 0.05s; }
        .sidebar ul li.has-submenu.open .submenu li:nth-child(2) { animation-delay: 0.1s; }

        .logout-btn { margin-top: auto; border-top: 1px solid rgba(255,255,255,0.1); padding-top: 15px; }
        .logout-btn a { color: #ff6b6b !important; transition: all 0.3s ease; }
        .logout-btn a:hover { background: rgba(255,107,107,0.1) !important; padding-left: 25px; }

        /* ===== USER INFO AVEC PHOTO ===== */
        .user-info {
            padding: 16px 18px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .user-avatar {
            width: 46px;
            height: 46px;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid rgba(255,255,255,0.35);
            flex-shrink: 0;
            transition: border-color 0.3s ease;
        }

        .user-avatar:hover { border-color: rgba(255,255,255,0.7); }

        .user-avatar-placeholder {
            width: 46px;
            height: 46px;
            border-radius: 50%;
            background: rgba(255,255,255,0.15);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            flex-shrink: 0;
            border: 2px solid rgba(255,255,255,0.2);
        }

        .user-details { flex: 1; min-width: 0; }
        .user-details strong { color: #fff; font-size: 14px; display: block; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
        .user-details .user-role { font-size: 11px; color: rgba(255,255,255,0.6); text-transform: uppercase; letter-spacing: 0.5px; margin-top: 2px; display: block; }
        .user-details .user-email { font-size: 11px; color: rgba(255,255,255,0.5); display: block; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }

        .user-profil-link {
            display: inline-block;
            margin-top: 4px;
            font-size: 11px;
            color: rgba(255,255,255,0.5);
            text-decoration: none;
            transition: color 0.2s;
        }
        .user-profil-link:hover { color: rgba(255,255,255,0.9); }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
<div class="layout">
    <aside class="sidebar">
        <h2>📦 PackTrack</h2>

        {% if app.user %}
            <div class="user-info">
                {% if app.user.photo %}
                    <img src="{{ asset('uploads/profils/' ~ app.user.photo) }}"
                         alt="Photo de profil"
                         class="user-avatar"
                         title="{{ app.user.prenom }} {{ app.user.nom }}">
                {% else %}
                    <div class="user-avatar-placeholder">👤</div>
                {% endif %}
                <div class="user-details">
                    <strong>{{ app.user.prenom }} {{ app.user.nom }}</strong>
                    <span class="user-role">{{ app.user.role.value }}</span>
                    <span class="user-email">{{ app.user.email }}</span>
                    <a href="{{ path('app_profil') }}" class="user-profil-link">
                        <i class="fas fa-pen fa-xs"></i> Mon profil
                    </a>
                </div>
            </div>
        {% endif %}

<ul>
    <li class="{{ app.request.get('_route') == 'admin_dashboard' ? 'active' }}">
        <a href="{{ path('admin_dashboard') }}"><i class="fa-solid fa-chart-line"></i> Dashboard</a>
    </li>
    <li class="{{ app.request.get('_route') starts with 'admin_parcels' ? 'active' }}">
        <a href="{{ path('admin_parcels') }}"><i class="fa-solid fa-box"></i> Colis</a>
    </li>
    <li class="{{ app.request.get('_route') starts with 'app_facture_' ? 'active' }}">
        <a href="{{ path('app_facture_index') }}"><i class="fa-solid fa-file-invoice"></i> Factures</a>
    </li>
    <li class="{{ app.request.get('_route') starts with 'admin_vehicles' ? 'active' }}">
        <a href="{{ path('admin_vehicles') }}"><i class="fa-solid fa-truck"></i> Vehicles</a>
    </li>
    <li class="{{ app.request.get('_route') starts with 'admin_forum' ? 'active' }}">
        <a href="{{ path('admin_forum') }}"><i class="fa-solid fa-comments"></i> Forum</a>
    </li>
    <li class="{{ app.request.get('_route') starts with 'admin_incidents' ? 'active' }}">
        <a href="{{ path('admin_incidents') }}"><i class="fa-solid fa-triangle-exclamation"></i> Incidents</a>
    </li>
    <li class="has-submenu {{ app.request.get('_route') starts with 'app_utilisateurs' ? 'active open' : '' }}">
        <a href="javascript:void(0);" onclick="toggleSubmenu(this)">
            <span><i class="fa-solid fa-users"></i> Utilisateurs</span>
            <i class="fa-solid fa-chevron-down arrow"></i>
        </a>
        <ul class="submenu">
            <li class="{{ app.request.get('_route') == 'app_utilisateurs' ? 'active' }}">
                <a href="{{ path('app_utilisateurs') }}"><i class="fa-solid fa-list"></i> Liste</a>
            </li>
            <li class="{{ app.request.get('_route') == 'app_utilisateurs_new' ? 'active' }}">
                <a href="{{ path('app_utilisateurs_new') }}"><i class="fa-solid fa-user-plus"></i> Ajout</a>
            </li>
            <li class="{{ app.request.get('_route') == 'app_utilisateurs_stats' ? 'active' }}">
                <a href="{{ path('app_utilisateurs_stats') }}"><i class="fas fa-chart-bar"></i> Statistiques</a>
            </li>
        </ul>
    </li>
    <li class="{{ app.request.get('_route') starts with 'app_notifications' ? 'active' }}">
        <a href="{{ path('app_notifications_admin') }}"><i class="fa-solid fa-bell"></i> Notifications</a>
    </li>
    <li class="logout-btn">
        <a href="{{ path('app_logout') }}"><i class="fa-solid fa-right-from-bracket"></i> Déconnexion</a>
    </li>
</ul>
</aside>

    <main class="main">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">{{ message }}</div>
            {% endfor %}
        {% endfor %}

            {% block body %}{% endblock %}
        </main>
    </div>

    

    <script>
        function toggleSubmenu(element) {
            const parentLi = element.parentElement
            parentLi.classList.toggle('open')
        }
    </script>

    {% block javascripts %}{% endblock %}

</body>
</html>
</body>
</html>
